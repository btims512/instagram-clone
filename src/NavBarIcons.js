import styled from "styled-components";

const NavBarIcons = styled.div`
  cursor: pointer;
  /* :hover {
    filter: drop-shadow(9px 10px 5px rgb(0 0 0 / 0.4));
    cursor: pointer;
  } */
`;

export default NavBarIcons;
